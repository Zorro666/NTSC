High
* NTSC - scaling values for smpte colour bars: gamma, brightness, contrast, IQ scaling
* NTSC - sort out colour burst angle offsets instead of +90, +270 - comment why those values are correct
* NTSC - Work out hacked in scaling for IQ values
* NTSC - use a filter generation program to find LPF weights: 0.25 (Y, I, Q), HSYNC, VSYNC long LPFs

Medium
* NTSC - Convert to fixed point instead of floats

Low
* Tint, brightness

Future
* Debug options: zoom, pan
* Debug options: single frame, frame step
* Debug options: graph window: h-sync, v-sync, h-lines
